#!/usr/bin/groovy
node {
       stage('write') {
  
         
               script {
                   def date = new Date()
                   def data = "Hello World\nSecond line\n" + date
                   writeFile(file: 'zorg.txt', text: data)
                   sh "ls -l"
                      
                   def datax = readFile(file: 'zorg.txt')
                   println(datax)
                      def datay = readFile(file: "${filePath}")
                    println(datay)
                   
                      
               }
           
       }

   }
